class PhraseMatcher:
    def __init__(self, phrases, vectors):
        self.phrases = phrases
        self.vectors = vectors

    def find_closest_match(self, input_phrase):
        #input_vec = get_phrase_embedding(input_phrase, model)
        #closest_phrase = None
        #min_distance = float('inf')
        #
        #for phrase in phrases:
        #    phrase_vec = get_phrase_embedding(phrase, model)
        #    dist = calculate_cosine_distance(input_vec, phrase_vec)
        #    if dist < min_distance:
        #        min_distance = dist
        #        closest_phrase = phrase
        #
        #return closest_phrase, min_distance
        pass